

exchange forum {
    type = direct
}

queue forum.comments.create {
    exchange = forum
    routing_key = forum.comments.create
}

queue forum.comments.update {
    exchange = forum
    routing_key = forum.comments.update
}